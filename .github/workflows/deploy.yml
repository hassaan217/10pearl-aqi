name: Deploy API and Dashboard

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        pip install fastapi uvicorn streamlit pymongo pandas python-dotenv
    
    - name: Test API
      run: |
        python -c "
import fastapi
print('✅ FastAPI available')
"
    
    - name: Deploy to server (example)
      env:
        DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
        DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      run: |
        echo "Deployment would happen here"
        # You can add actual deployment commands like:
        # rsync -avz -e "ssh -i key" ./ user@server:/app